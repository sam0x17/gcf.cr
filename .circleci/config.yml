version: 2
jobs:
  test:
    docker:
      - image: jrei/crystal-alpine
    steps:
      - checkout
      - run: shards install
      - run: crystal spec
workflows:
  version: 2
  test:
    jobs:
      - test
